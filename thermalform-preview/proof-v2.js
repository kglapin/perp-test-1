(()=>{const proof=document.querySelector('.proof-strip');if(!proof||proof.querySelector('.proof-strip__layout'))return;const grid=proof.querySelector('.proof-strip__grid');if(!grid)return;const layout=document.createElement('div');layout.className='proof-strip__layout';const intro=document.createElement('div');intro.className='proof-strip__intro';intro.innerHTML='<span class="eyebrow" data-index="00A">Working standard</span><h2>Four checkpoints before the work is complete.</h2><p>These are the practical controls that keep the job understandable from survey through to handover — separate from the customer feedback above.</p>';proof.insertBefore(layout,grid);layout.append(intro,grid);})();