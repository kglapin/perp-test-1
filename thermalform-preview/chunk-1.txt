<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <meta name="description" content="Professional spray foam insulation for homes and buildings across Ireland. Site assessment, ventilation planning and documented installation." />
  <title>ThermalForm | Spray Foam Insulation Ireland</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Manrope:wght@400;500;600;700&display=swap" rel="stylesheet">
  <style>
    :root{
      --canvas:#f4f3ed;
      --surface:#ffffff;
      --ink:#111712;
      --forest:#173c2e;
      --forest-2:#0d2a20;
      --foam:#c9f35a;
      --foam-2:#e0ff8f;
      --muted:#69736c;
      --line:#d8ddd7;
      --line-dark:rgba(255,255,255,.16);
      --max:1280px;
      --gutter:clamp(16px,3vw,32px);
      --section:clamp(76px,10vw,136px);
      --header:78px;
      --radius-sm:10px;
      --radius-md:18px;
      --radius-lg:28px;
      --shadow:0 24px 70px rgba(17,23,18,.09);
    }
    *{box-sizing:border-box}
    html{scroll-behavior:smooth;background:var(--canvas)}
    body{margin:0;color:var(--ink);background:var(--canvas);font-family:Manrope,system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif;font-size:16px;line-height:1.6;-webkit-font-smoothing:antialiased}
    body.menu-open{overflow:hidden}
    a{color:inherit;text-decoration:none}
    img{display:block;max-width:100%}
    button,input,select,textarea{font:inherit}
    button{cursor:pointer}
    :focus-visible{outline:3px solid var(--foam);outline-offset:4px}
    .container{width:min(calc(100% - var(--gutter)*2),var(--max));margin-inline:auto}
    .eyebrow{display:inline-flex;align-items:center;min-height:34px;padding-left:48px;position:relative;font-size:.67rem;font-weight:700;line-height:1.15;letter-spacing:.14em;text-transform:uppercase;color:#465249}
    .eyebrow:before{content:attr(data-index);position:absolute;left:0;top:50%;transform:translateY(-52%);font-family:ui-monospace,SFMono-Regular,Menlo,monospace;font-size:1.55rem;font-weight:500;letter-spacing:-.12em;line-height:1;color:var(--forest)}
    .eyebrow:after{content:"/";position:absolute;left:31px;top:50%;transform:translateY(-53%);font-family:ui-monospace,SFMono-Regular,Menlo,monospace;font-size:.82rem;color:#a2aaa4}
    .eyebrow--light{color:rgba(255,255,255,.66)}
    .eyebrow--light:before{color:var(--foam)}
    .eyebrow--light:after{color:rgba(255,255,255,.28)}
    .section-title{margin:16px 0 0;font-size:clamp(2.25rem,4.6vw,4.6rem);line-height:1.01;letter-spacing:-.045em;font-weight:600;max-width:15ch}
    .section-copy{margin:24px 0 0;font-size:clamp(1rem,1.4vw,1.16rem);color:var(--muted);max-width:64ch}
    .button{min-height:52px;padding:0 22px;border-radius:999px;border:1px solid transparent;display:inline-flex;align-items:center;justify-content:center;gap:10px;font-weight:700;font-size:.92rem;letter-spacing:-.01em;transition:transform .18s ease,background-color .18s ease,border-color .18s ease,color .18s ease}
    .button:hover{transform:translateY(-2px)}
    .button--primary{background:var(--foam);color:#0d160f}
    .button--primary:hover{background:var(--foam-2)}
    .button--dark{background:var(--ink);color:#fff}
    .button--ghost{border-color:var(--line);background:transparent}
    .button svg{width:18px;height:18px}
    .reveal{opacity:0;transform:translateY(26px);transition:opacity .72s cubic-bezier(.2,.75,.2,1),transform .72s cubic-bezier(.2,.75,.2,1)}
    .reveal.is-visible{opacity:1;transform:none}

    /* HEADER */
    .site-header{position:fixed;z-index:60;left:0;right:0;top:0;height:var(--header);display:flex;align-items:center;border-bottom:1px solid transparent;transition:background .25s ease,border-color .25s ease,backdrop-filter .25s ease}
    .site-header.scrolled{background:rgba(244,243,237,.88);backdrop-filter:blur(18px);border-bottom-color:rgba(17,23,18,.08)}
    .nav{display:flex;align-items:center;justify-content:space-between;gap:24px}
    .logo{display:grid;gap:2px;align-items:center;white-space:nowrap;line-height:1}
    .logo-word{font-size:1rem;font-weight:700;letter-spacing:-.045em}
    .logo-sub{font-size:.49rem;font-weight:700;letter-spacing:.16em;text-transform:uppercase;color:var(--muted)}
    footer .logo-sub{color:rgba(255,255,255,.38)}
    .nav-links{display:flex;align-items:center;gap:30px;font-size:.82rem;font-weight:600}
    .nav-links a{position:relative}
    .nav-links a:after{content:"";position:absolute;left:0;right:100%;bottom:-7px;height:1px;background:var(--ink);transition:right .2s ease}
    .nav-links a:hover:after{right:0}
    .nav-actions{display:flex;align-items:center;gap:18px}
    .phone{font-size:.82rem;font-weight:700}
    .menu-toggle{display:none;border:0;background:none;width:44px;height:44px;padding:0;align-items:center;justify-content:center;position:relative}
    .menu-toggle span,.menu-toggle:before,.menu-toggle:after{content:"";position:absolute;width:22px;height:1.5px;background:var(--ink);transition:transform .2s ease,opacity .2s ease}
    .menu-toggle span{transform:translateY(0)}
    .menu-toggle:before{transform:translateY(-7px)}
    .menu-toggle:after{transform:translateY(7px)}
    .menu-open .menu-toggle span{opacity:0}
    .menu-open .menu-toggle:before{transform:rotate(45deg)}
    .menu-open .menu-toggle:after{transform:rotate(-45deg)}
    .mobile-panel{position:fixed;z-index:55;inset:0;background:var(--canvas);padding:calc(var(--header) + 32px) var(--gutter) 32px;display:none;flex-direction:column;justify-content:space-between}
    .mobile-panel a{display:block;font-size:clamp(2rem,10vw,3.4rem);font-weight:600;letter-spacing:-.04em;line-height:1.25;padding:5px 0}
    .mobile-panel-bottom{display:grid;gap:14px}

    /* HERO */
    .hero{padding:calc(var(--header) + clamp(50px,7vw,92px)) 0 clamp(78px,8vw,112px);min-height:min(900px,100vh);display:flex;align-items:center;overflow:hidden;position:relative}
    .hero:after{content:"";position:absolute;width:44vw;height:44vw;border-radius:50%;background:rgba(201,243,90,.17);filter:blur(50px);right:-20vw;top:-20vw;pointer-events:none}
    .hero-grid{display:grid;grid-template-columns:minmax(0,1.05fr) minmax(430px,.82fr);gap:clamp(44px,7vw,108px);align-items:center}
    .hero-copy{position:relative;z-index:2}
    .hero h1{margin:18px 0 0;font-size:clamp(3.7rem,7.2vw,7rem);line-height:.92;letter-spacing:-.07em;font-weight:600;max-width:8ch;text-wrap:balance}
    .hero-lead{font-size:clamp(1.05rem,1.5vw,1.22rem);max-width:57ch;color:var(--muted);margin:30px 0 0}
    .hero-actions{display:flex;align-items:center;gap:18px;margin-top:34px;flex-wrap:wrap}
    .microcopy{font-size:.77rem;color:var(--muted);max-width:33ch;line-height:1.45}
    .hero-trust{display:flex;flex-wrap:wrap;gap:10px 22px;margin-top:44px;padding-top:22px;border-top:1px solid var(--line);font-size:.75rem;font-weight:700;color:#465249}
    .trust-item{display:flex;align-items:center;gap:8px}
    .trust-item:before{content:"";width:7px;height:7px;background:var(--forest);border-radius:50%}
    .hero-visual{height:clamp(540px,68vh,710px);position:relative;z-index:2}
    .hero-photo-wrap{position:absolute;inset:0 0 0 6%;overflow:hidden;border-radius:45% 45% 18px 18px / 28% 28% 18px 18px;background:var(--forest);box-shadow:var(--shadow)}
    .hero-photo{width:100%;height:100%;object-fit:cover;filter:saturate(.82) contrast(.98);transform:scale(1.06);animation:heroImage 1.1s cubic-bezier(.2,.8,.2,1) .1s both}
    @keyframes heroImage{from{transform:scale(1.13);opacity:.3}to{transform:scale(1.06);opacity:1}}
    .hero-overlay{position:absolute;inset:auto -24px 46px auto;background:var(--forest);color:#fff;padding:22px 24px;border-radius:var(--radius-md);max-width:255px;box-shadow:0 18px 50px rgba(13,42,32,.22)}
    .hero-overlay strong{display:block;font-size:1.15rem;line-height:1.15;letter-spacing:-.03em}
    .hero-overlay span{display:block;color:rgba(255,255,255,.68);font-size:.75rem;margin-top:8px;line-height:1.45}
    .foam-orbit{position:absolute;left:-32px;top:66px;width:96px;height:96px;background:var(--foam);border-radius:49% 51% 53% 47% / 59% 43% 57% 41%;z-index:3;animation:blob 9s ease-in-out infinite}
    .foam-orbit:before,.foam-orbit:after{content:"";position:absolute;background:var(--foam);border-radius:50%}
    .foam-orbit:before{width:38px;height:38px;right:-10px;bottom:7px}.foam-orbit:after{width:23px;height:23px;left:-5px;top:4px}
    @keyframes blob{0%,100%{transform:rotate(0) scale(1)}50%{transform:rotate(8deg) scale(1.05)}}

    /* BUILDING ENVELOPE */
    .problem{padding:var(--section) 0;background:var(--surface)}
    .problem-head{display:grid;grid-template-columns:minmax(0,1fr) minmax(300px,.58fr);gap:clamp(48px,8vw,110px);align-items:end;margin-bottom:clamp(52px,6vw,82px)}
    .problem-copy .section-title{max-width:13ch}
    .problem-head .section-copy{margin:0}
    .envelope-board{display:grid;grid-template-columns:minmax(0,1.45fr) minmax(340px,.62fr);min-height:640px;background:var(--forest-2);color:#fff;border-radius:var(--radius-lg);overflow:hidden;box-shadow:var(--shadow)}
    .envelope-media{position:relative;min-height:640px;overflow:hidden}
    .envelope-media:after{content:"";position:absolute;inset:0;background:linear-gradient(180deg,rgba(6,16,11,.02),rgba(6,16,11,.42))}
    .house-photo{position:absolute;inset:0;width:100%;height:100%;object-fit:cover;filter:saturate(.78) contrast(.98)}
    .media-index{position:absolute;z-index:2;left:26px;top:24px;display:flex;align-items:center;gap:10px;font-size:.65rem;font-weight:700;letter-spacing:.13em;text-transform:uppercase;color:rgba(255,255,255,.76)}
    .media-index:before{content:"";width:8px;height:8px;background:var(--foam)}
    .envelope-thesis{position:absolute;z-index:2;left:26px;right:26px;bottom:26px;display:grid;grid-template-columns:1fr auto;gap:28px;align-items:end}
    .envelope-thesis strong{font-size:clamp(1.6rem,2.6vw,2.6rem);font-weight:550;letter-spacing:-.045em;line-height:1.03;max-width:14ch}
    .envelope-thesis span{font-size:.72rem;line-height:1.5;color:rgba(255,255,255,.62);max-width:26ch}
    .envelope-panel{padding:34px 34px 28px;display:flex;flex-direction:column;justify-content:space-between;background:#10251c;border-left:1px solid rgba(255,255,255,.1)}
    .envelope-panel-head small{display:block;font-size:.62rem;letter-spacing:.14em;text-transform:uppercase;color:rgba(255,255,255,.42)}
    .envelope-panel-head h3{font-size:clamp(1.55rem,2.2vw,2.25rem);line-height:1.06;letter-spacing:-.04em;font-weight:550;margin:14px 0 0;max-width:11ch}
    .envelope-list{margin-top:40px}
    .envelope-point{display:grid;grid-template-columns:38px 1fr;gap:14px;padding:20px 0;border-top:1px solid rgba(255,255,255,.12)}
    .envelope-point:last-child{border-bottom:1px solid rgba(255,255,255,.12)}
    .envelope-point b{font-size:.63rem;letter-spacing:.08em;color:var(--foam);font-weight:700}
    .envelope-point strong{display:block;font-size:.92rem;font-weight:600;letter-spacing:-.02em}
    .envelope-point p{font-size:.72rem;line-height:1.5;color:rgba(255,255,255,.52);margin:5px 0 0}
    .envelope-note{margin-top:30px;font-size:.7rem;line-height:1.55;color:rgba(255,255,255,.58);max-width:34ch}

    /* INSTALLATION METHOD STORY */
    .story-section{padding:var(--section) 0;background:var(--canvas)}
    .story-header{display:grid;grid-template-columns:1fr .68fr;gap:50px;align-items:end;margin-bottom:clamp(58px,7vw,100px)}
    .story-header .section-copy{margin:0}
    .story-grid{display:grid;grid-template-columns:minmax(0,1.12fr) minmax(360px,.68fr);gap:clamp(54px,8vw,112px);align-items:start}
    .roof-sticky{position:sticky;top:calc(var(--header) + 28px);height:min(75vh,720px);min-height:590px;border-radius:var(--radius-lg);overflow:hidden;background:#0a1710;color:#fff;box-shadow:var(--shadow)}
    .method-topbar{posit